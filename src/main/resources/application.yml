Spring:
  datasource:
    url: jdbc:mysql://localhost:3306/studyManager
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
    properties:
      hibernate:
        format-sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect


